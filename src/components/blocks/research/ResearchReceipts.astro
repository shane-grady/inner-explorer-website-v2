---
export interface Study {
  id: string;
  year: number;
  tag: string;
  title: string;
  authors: string;
  institution: string;
  journal: string;
  leadStat: string;
  leadLabel: string;
}
interface Props {
  chapterLabel: string;
  chapterNumber: string;
  headingHtml: string;
  lede: string;
  studies: Study[];
}
const { chapterLabel, chapterNumber, headingHtml, lede, studies }: Props = Astro.props;
const sorted = [...studies].sort((a, b) => a.year - b.year);
---

<section
  class="receipts"
  id="receipts"
  data-screen-label={chapterLabel}
  aria-labelledby="receipts-heading"
>
  <div class="wrap">
    <div class="chap-mark reveal">
      <span class="chap-num">{chapterNumber}</span>
      <span class="chap-rule" aria-hidden="true"></span>
      <span class="chap-label">{chapterLabel}</span>
    </div>
    <div class="intro reveal">
      <h2 class="chap-title" id="receipts-heading" set:html={headingHtml} />
      <p class="lede">{lede}</p>
    </div>
    <ol class="rows">
      {
        sorted.map((s) => {
          const doctoral = s.tag.toLowerCase().includes('doctoral');
          return (
            <li class="row reveal" id={`r-${s.id}`}>
              <div class="row-result">
                <div class="row-result-stat">{s.leadStat}</div>
                <p class="row-result-label">{s.leadLabel}</p>
              </div>
              <div class="row-body">
                <p class="row-kicker">
                  <span class:list={['tag', { doctoral }]}>{s.tag}</span>
                  <span class="row-year">{s.year}</span>
                </p>
                <h3 class="row-title">{s.title}</h3>
                <p class="row-meta">
                  <strong>{s.authors}</strong> · {s.institution} · <em>{s.journal}</em>
                </p>
              </div>
            </li>
          );
        })
      }
    </ol>
  </div>
</section>

<style>
  .receipts {
    position: relative;
    padding: 140px 0 120px;
    background: var(--editorial-paper);
    color: var(--editorial-ink);
  }
  .wrap {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 32px;
  }
  .chap-mark {
    display: flex;
    align-items: baseline;
    gap: 18px;
    margin-bottom: 28px;
  }
  .chap-num {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 500;
    font-size: 36px;
    line-height: 1;
    color: var(--color-brand);
  }
  .chap-rule {
    flex: 1;
    height: 1px;
    background: currentColor;
    opacity: 0.18;
    align-self: center;
  }
  .chap-label {
    font-size: 11px;
    font-weight: 500;
    line-height: 1;
    letter-spacing: 0.28em;
    text-transform: uppercase;
    opacity: 0.55;
  }
  .intro {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 64px;
    align-items: end;
    margin-bottom: 80px;
  }
  .chap-title {
    margin: 0;
    font-family: var(--font-display);
    font-weight: 400;
    font-size: clamp(44px, 6vw, 88px);
    line-height: 1.02;
    letter-spacing: -0.02em;
    text-wrap: balance;
  }
  .chap-title :global(em) {
    font-style: normal;
    color: var(--color-brand);
    font-weight: 500;
  }
  .chap-title :global(.sans) {
    font-family: var(--font-sans);
    font-style: normal;
    font-weight: 300;
    letter-spacing: -0.025em;
  }
  .chap-title :global(.stk) {
    display: block;
  }
  .lede {
    margin: 0 0 8px;
    max-width: 480px;
    font-size: 19px;
    line-height: 1.6;
    color: var(--editorial-ink-muted);
  }

  .rows {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
  }
  .row {
    display: grid;
    grid-template-columns: minmax(220px, 300px) 1fr;
    gap: 56px;
    align-items: center;
    padding: 44px 0;
    border-top: 1px solid color-mix(in oklab, var(--editorial-ink) 12%, transparent);
    transition: background 400ms var(--ease-out);
  }
  .row:last-child {
    border-bottom: 1px solid color-mix(in oklab, var(--editorial-ink) 12%, transparent);
  }
  .row:hover {
    background: color-mix(in oklab, var(--editorial-ink) 3%, transparent);
  }
  .row:hover .row-result-stat {
    transform: translateX(-4px);
  }

  /* Lead column — the result. The visual anchor of each row. */
  .row-result {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .row-result-stat {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 500;
    font-size: clamp(56px, 6.5vw, 104px);
    line-height: 0.92;
    letter-spacing: -0.03em;
    color: var(--color-brand);
    transition: transform 400ms var(--ease-out);
  }
  .row-result-label {
    margin: 0;
    max-width: 280px;
    font-size: 14px;
    font-weight: 500;
    line-height: 1.45;
    color: var(--editorial-ink-muted);
  }

  /* Content column — title leads as the heading; the date is a quiet kicker. */
  .row-body {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .row-kicker {
    display: flex;
    align-items: center;
    gap: 16px;
    margin: 0;
  }
  .row-year {
    font-size: 13px;
    font-weight: 500;
    line-height: 1;
    letter-spacing: 0.16em;
    color: var(--editorial-ink-muted);
    font-variant-numeric: tabular-nums;
  }
  .tag {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 10px;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-brand);
  }
  .tag::before {
    content: '';
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--color-brand);
    box-shadow: 0 0 0 3px color-mix(in oklab, var(--color-brand) 18%, transparent);
  }
  .tag.doctoral {
    color: var(--editorial-ink-muted);
  }
  .tag.doctoral::before {
    background: var(--editorial-ink-muted);
    box-shadow: 0 0 0 3px color-mix(in oklab, var(--editorial-ink) 8%, transparent);
  }
  .row-title {
    margin: 0;
    font-family: var(--font-display);
    font-style: normal;
    font-weight: 500;
    font-size: clamp(24px, 2.6vw, 34px);
    line-height: 1.12;
    letter-spacing: -0.01em;
    color: var(--editorial-ink);
    text-wrap: balance;
  }
  .row-meta {
    margin: 2px 0 0;
    font-size: 13px;
    line-height: 1.45;
    color: var(--editorial-ink-muted);
  }
  .row-meta strong {
    color: var(--editorial-ink);
    font-weight: 500;
  }

  /* Reveal-on-scroll — content is fully visible by default. When JS is ready
     (intersect.ts sets data-js-ready on <html>), not-yet-revealed items hide
     to their initial state; the .in class then animates them back. */
  .reveal {
    opacity: 1;
    transform: none;
    transition:
      opacity 900ms var(--ease-out),
      transform 900ms var(--ease-out);
  }
  :global([data-js-ready]) .reveal:not(.in) {
    opacity: 0;
    transform: translateY(28px);
  }

  @media (max-width: 820px) {
    .intro {
      grid-template-columns: 1fr;
      gap: 24px;
    }
    .row {
      grid-template-columns: 1fr;
      gap: 16px;
      padding: 32px 0;
    }
    .row-result-stat {
      font-size: 64px;
    }
    .row-result-label {
      max-width: none;
    }
  }
</style>

<script>
  import { observe } from '../../../lib/intersect';
  observe('.receipts .reveal');
</script>
